export enum FormMode {
  EDIT = 'edit',
  DRAFT = 'draft',
  CREATE = 'create',
}

export enum FilterTaskMode {
  HOME = 'home',
  COMPLETED = 'completed',
}

export enum TaskItemMode {
  DEFAULT = 'task',
  DRAFT = 'draft',
}
